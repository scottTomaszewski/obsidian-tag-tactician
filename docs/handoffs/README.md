# docs/handoffs/

Session handoffs live here as `HANDOFF.md`, written/read by the `creating-handoffs`
(`/handoff`) skill — an ephemeral "you are here" router for resuming in-progress work.
Don't put durable docs here; those belong in `ARCHITECTURE.md` or `docs/`.
